package io.github.tagdl.RebarTranscEndence.items;

import org.bukkit.inventory.ItemStack;
import org.jetbrains.annotations.NotNull;

public class ZotUp extends Zot {
    public ZotUp(@NotNull ItemStack stack) {
        super(stack);
    }
}
