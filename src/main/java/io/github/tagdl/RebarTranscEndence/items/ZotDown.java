package io.github.tagdl.RebarTranscEndence.items;

import org.bukkit.inventory.ItemStack;
import org.jetbrains.annotations.NotNull;

public class ZotDown extends Zot {
    public ZotDown(@NotNull ItemStack stack) {
        super(stack);
    }
}
